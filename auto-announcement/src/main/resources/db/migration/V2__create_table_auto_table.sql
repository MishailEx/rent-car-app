CREATE TABLE auto (
                      id SERIAL PRIMARY KEY,
                      car_year INTEGER NOT NULL,
                      category_id INTEGER,
                      mark_id INTEGER,
                      model_id INTEGER
);

